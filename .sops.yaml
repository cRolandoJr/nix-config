# El recipient es la identidad age derivada de ~/.ssh/id_ed25519 (ssh-to-age).
# Elegido así para no tener una segunda clave que respaldar.
keys:
  - &rolando age1chjr3jrprx3ljqcmnvhkwh74v83cp2jdggygtps5hklur6t0fp2qn4gdll

creation_rules:
  - path_regex: secrets/.*
    key_groups:
      - age:
          - *rolando
